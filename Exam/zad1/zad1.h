#ifndef ZAD1_H
#define ZAD1_H

#include <stdio.h>
#include <stdlib.h>
#include <time.h>

char randLettersInterval(const char, const char);
void printArray(const char*, size_t);
unsigned int smallLetters(const char*, size_t);
char mostCommonSymbol(const char*, size_t);

#endif //ZAD1_H